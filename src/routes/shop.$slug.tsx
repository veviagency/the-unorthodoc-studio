import { createFileRoute, notFound } from "@tanstack/react-router";
import { DemoAction } from "@/components/forms";
import { MobileStickyCta, PageHero } from "@/components/page-kit";
import { products } from "@/lib/site-data";
export const Route=createFileRoute("/shop/$slug")({loader:({params})=>{const product=products.find(p=>p.slug===params.slug);if(!product)throw notFound();return {product}},head:({loaderData})=>({meta:[{title:loaderData?`${loaderData.product.title} — The UnOrthoDoc Shop`:"Product unavailable — The UnOrthoDoc"},{name:"description",content:loaderData?.product.text??"This product is not currently available."},{property:"og:title",content:loaderData?.product.title??"Product unavailable"},{property:"og:description",content:loaderData?.product.text??"This product is not currently available."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Product});
function Product(){const {product}=Route.useLoaderData();return <><PageHero eyebrow={product.kind} title={product.title} intro={product.text}><DemoAction label={product.price==="Coming soon"?"Join the interest list":"Purchase — demo"}/></PageHero><section className="section"><div className="site-container course-layout"><div><span className="eyebrow">Made for real schedules</span><h2>A useful resource, without the overwhelm.</h2><p>This product page is ready for final imagery, detailed contents, delivery information, and a live payment link.</p></div><div className="membership-card"><span className="eyebrow">Product details</span><h3>{product.title}</h3><div className="price">{product.price}</div><p>Digital fulfillment and checkout will be connected at launch. No card details are collected in this prototype.</p><DemoAction label="Continue — demo"/></div></div></section><MobileStickyCta label="Continue — demo"/></>}
